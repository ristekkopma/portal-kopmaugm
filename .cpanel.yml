---
deployment:
  tasks:
    - export DEPLOYPATH=/home/u1656986/public_html/repositories/portal-kopmaugm.com/
    - /usr/bin/rsync -a --delete --exclude=.git --exclude=.env --exclude=vendor --exclude=storage --exclude=node_modules ./ $DEPLOYPATH
    - cd $DEPLOYPATH && php artisan optimize:clear
